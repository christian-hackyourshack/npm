{
  "name": "npm",
  "version": "0.0.0",
  "private": true,
  "homepage": "https://github.com/christian-hackyourshack/npm",
  "repository": {
    "type": "git",
    "url": "https://github.com/christian-hackyourshack/npm.git"
  },
  "scripts": {
    "build": "turbo run build --no-daemon",
    "clean": "turbo run clean --no-daemon && rimraf -rf node_modules/.cache/turbo",
    "dev": "turbo run dev --parallel",
    "dev:reset": "turbo run clean --no-daemon && rimraf -f pnpm-lock.yaml && rimraf -rf .pnpm-store && rimraf -rf ./**/node_modules/",
    "format": "prettier --write \"**/*.{astro,cjs,js,json,jsx,md,mdx,mjs,ts,tsx}\"",
    "lint": "turbo run lint --no-daemon",
    "prepare:changeset": "changeset",
    "release:build": "turbo run build --filter=!./apps/* --no-daemon",
    "release:check": "pnpm install && turbo run lint test --filter=!./apps/* --no-daemon",
    "release:version": "changeset version",
    "release:publish": "changeset publish",
    "test": "pnpm install && turbo run test --no-daemon",
    "test:dev": "turbo run test:dev --no-daemon"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@7.13.6",
  "dependencies": {},
  "devDependencies": {
    "@changesets/changelog-github": "^0.4.7",
    "@changesets/cli": "^2.25.0",
    "eslint-config-base": "workspace:*",
    "pnpm": "latest",
    "prettier": "^2.7.1",
    "prettier-plugin-astro": "^0.6.0",
    "rimraf": "^3.0.2",
    "turbo": "^1.5.6"
  }
}
