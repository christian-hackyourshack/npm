---
import { twMerge } from 'tailwind-merge';
export type Props = astroHTML.JSX.HTMLAttributes;
const { class: className = '', ...props } = Astro.props;
---

<h2 class={twMerge('h2', className)} {...props}>
  <slot />
</h2>
